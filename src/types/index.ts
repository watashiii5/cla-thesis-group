export interface Campus {
  id: string;
  name: string;
}

export interface Building {
  id: string;
  name: string;
}

export interface Room {
  id: string;
  capacity: string;
}

export interface Applicant {
  appNo: string;
  course: string;
  major: string;
  time: string;
}